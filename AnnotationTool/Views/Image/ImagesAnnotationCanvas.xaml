<UserControl x:Class="AnnotationTool.Views.Image.ImagesAnnotationCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"          
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="#303030">
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="CanvasRegion"></ContentControl>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ImagesControlsRegion"></ContentControl>

    </Grid>
</UserControl>
