<UserControl x:Class="AnnotationTool.Views.Video.VideoAnnotationCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="#343434">
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="CanvasRegion"></ContentControl>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="VideoControlsRegion"></ContentControl>

    </Grid>
</UserControl>
